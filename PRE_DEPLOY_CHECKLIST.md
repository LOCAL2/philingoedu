# Pre-Deploy Testing Checklist — Philingo

> ใช้ checklist นี้ทุกครั้งก่อนกด Deploy/Publish จริง ห้ามข้าม แม้ AI จะบอกว่า "แก้เสร็จแล้ว"

---

## ขั้นตอนที่ 0: ยืนยัน Deploy Readiness (ทำก่อนเสมอ)

```
สั่ง AI:
1. เทียบ timestamp ของทุกไฟล์ที่แก้ไขในเซสชันนี้ กับ timestamp ของ deploy ล่าสุด
2. แสดงเป็นตาราง: [ไฟล์] → [เวลาที่แก้ไขล่าสุด] → [รวมอยู่ใน deploy ล่าสุดหรือไม่]
3. ถ้ามีไฟล์ไหนที่แก้ไขแล้วแต่ยังไม่ได้ deploy ให้ deploy ทันทีก่อนทดสอบขั้นตอนถัดไป
```

---

## ขั้นตอนที่ 1: ทดสอบทุกขนาดจอ (ทำทุกหน้าที่แก้ไข)

ใช้ DevTools (F12 → Device Toolbar) ทดสอบที่ขนาดเหล่านี้ทุกครั้ง:

| ขนาดจอ | อุปกรณ์ตัวแทน |
|---|---|
| 320px | iPhone SE (จอเล็กสุด) |
| 360px | Android ทั่วไป |
| 375px | iPhone มาตรฐาน |
| 390px | iPhone รุ่นใหม่ |
| 768px | iPad (แนวตั้ง) |
| 1280px+ | Desktop |

**สำหรับแต่ละขนาดจอ เช็คว่า:**
- [ ] ไม่มี horizontal scroll ทั้งหน้า (เนื้อหาไม่ล้นขอบจอ)
- [ ] ตัวหนังสือทุกจุดอ่านออก ไม่เล็ก/ใหญ่เกินไป ไม่ถูกตัด
- [ ] ปุ่ม/ลิงก์ทุกอันกดได้ ไม่มีอะไรบัง
- [ ] รูปภาพขึ้นครบทุกรูป ไม่มีไอคอนหักมุม

---

## ขั้นตอนที่ 2: Checklist เฉพาะจุดที่เคยพัง (เช็คซ้ำทุกครั้ง)

### หน้าแรก
- [ ] Banner กิจกรรม/สัมมนา คลิกได้ ลิงก์ไปหน้าที่ถูกต้อง
- [ ] รูปภาพ banner ขึ้นครบ ไม่มีไอคอนหักมุม

### หน้าสัมมนา (/seminars)
- [ ] Mobile Hero: overlay (นับถอยหลัง + CTA) ทับอยู่บนภาพ ไม่หลุดไปอยู่ใต้ภาพ
- [ ] สัดส่วนภาพ:overlay สมดุล เห็นภาพพื้นหลังอย่างน้อย 50%
- [ ] ตัวหนังสือใน countdown/pills/CTA อ่านออกชัดเจนที่ 320px
- [ ] `/activities` redirect มาที่ `/seminars` อัตโนมัติ ไม่ error 404

### หน้าสถาบัน (School page)
- [ ] ตัวเลือกแผนเรียน/ห้องพัก/ระยะเวลา เป็น dropdown (ไม่ใช่ปุ่มแถวแนวนอน)
- [ ] ราคาประเมิน/ราคาโปรโมชั่น แสดงในจอพอดี ไม่ล้นขวา ไม่ต้องเลื่อนดู
- [ ] ตารางเปรียบเทียบราคา / ตารางเรียนรายวัน ลาก (swipe) ดูได้ ไม่ล้นจอค้าง
- [ ] กล่อง "สีตาราง/กฎของโรงเรียน" อยู่ตำแหน่งถูกต้อง ไม่ลอยผิดที่
- [ ] วิดีโอสถาบัน (เช่น CIA) แสดงผลได้ปกติ

### หน้ากิจกรรม (Activity)
- [ ] เนื้อหาตรงกับที่ตั้งค่าไว้ล่าสุด ไม่ใช้ค่า default/เปลี่ยนเอง
- [ ] แสดงผลตรงกันทั้ง Desktop และ Mobile (เนื้อหาเดียวกัน จัดวางต่างกันได้)

### หน้ารีวิว
- [ ] จำนวนรีวิวตรงกับที่มีใน Admin Panel
- [ ] คลิกดูรีวิวแล้วมีข้อมูลแสดงถูกต้อง

### ทุกหน้า (ปุ่มลอย)
- [ ] ปุ่มลอย (LINE/Messenger/TikTok/โทร) ไม่บังเนื้อหาสำคัญบนจอเล็ก (320-375px)

### ระบบภาษา TH/EN
- [ ] เลือกภาษาไทย → refresh หน้า → ยังเป็นไทย (ไม่ reset เป็นอังกฤษ)
- [ ] เลือกภาษาไทย → เปลี่ยนหน้า → กลับมา → ยังเป็นไทย

### Admin Panel
- [ ] SEO Title/Description กรอกแล้ว refresh ยังอยู่ (ไม่หาย)
- [ ] Social links (LINE, Facebook) แก้แล้วหน้าเว็บจริงอัปเดตตาม ไม่เด้งกลับค่าเดิม

---

## ขั้นตอนที่ 3: ทดสอบบน Production จริง (ไม่ใช่แค่ Preview)

1. เปิด browser **Incognito/Private mode ใหม่** (กัน cache หลอกตา)
2. เข้า domain จริง (philingoedu.com) ไม่ใช่ preview URL ของ Replit
3. กด `Ctrl+Shift+R` (hard refresh)
4. ไล่เช็คตามขั้นตอนที่ 1-2 ซ้ำอีกครั้งบน production จริง

---

## ขั้นตอนที่ 4: ทดสอบ Restart Server (เช็คว่าข้อมูล/การตั้งค่าไม่หาย)

```
สั่ง AI:
1. Restart server 2-3 รอบ
2. หลังแต่ละรอบ เช็คว่า: จำนวนรีวิว, เนื้อหาหน้ากิจกรรม, การตั้งค่า SEO/Social links 
   ยังคงค่าเดิมไม่เปลี่ยนแปลง
3. รายงานผลเทียบก่อน-หลัง restart แต่ละรอบ
```

---

## ขั้นตอนที่ 5: บันทึกผล

หลังผ่านทุกข้อแล้ว ให้บันทึกลง `REGRESSION_TEST_CHECKLIST.md`:
```
วันที่: [วันที่ทดสอบ]
สิ่งที่แก้ไข: [สรุปสั้นๆ]
ผลทดสอบ: ผ่านครบ / มีจุดที่ยังไม่ผ่าน (ระบุ)
Deploy timestamp: [เวลาที่ deploy ล่าสุด]
```

---

## กฎทอง: ห้ามเชื่อคำว่า "แก้เสร็จแล้ว" จนกว่าจะ...

1. เห็น diff จริง
2. เห็นหลักฐาน deploy timestamp ใหม่กว่าการแก้ไข
3. เปิด Incognito เช็คด้วยตาตัวเองบน production จริง
4. ทดสอบผ่านครบตาม checklist ข้างบน
